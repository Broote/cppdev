<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.text_area :body,:rows => 5 %>
  </div>
  <div class="actions">
    <%= f.submit "Добавить комментарий" %>
  </div>
<% end %>
