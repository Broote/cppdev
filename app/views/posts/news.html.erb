<h1>News</h1>
<table>
  <tr>
    <th>User</th>
    <th>Title</th>
    <th>Commentable</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @news.each do |post| %>
      <tr>
        <td><%= post.user.first_name %></td>
        <td><%= post.title %></td>
        <td><%= post.commentable %></td>

        <td><%= link_to 'Показать', post %></td>
        <% if can? :update, post %>
            <td><%= link_to 'Изменить', edit_post_path(post) %></td>
            <td><%= link_to 'Удалить', post, :confirm => 'Вы уверены?', :method => :delete %></td>
        <% end %>
      </tr>
  <% end %>
</table>
<% if can? :create, Post %>
    <br/><%= link_to 'Новое сообщение', new_post_path %>
<% end %>